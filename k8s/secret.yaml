apiVersion: v1
kind: Secret
metadata:
  name: selenium-mcp-secrets
  namespace: selenium-mcp
  labels:
    app.kubernetes.io/name: selenium-mcp
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Redis password (replace with actual value or use external secret management)
  REDIS_PASSWORD: ""
  
  # Optional: Zipkin/tracing authentication
  # ZIPKIN_AUTH_TOKEN: ""

